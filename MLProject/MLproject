name: wine_quality_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: { type: str, default: winequality_preprocessed_train.csv }
    command: >
      python modelling.py --data_path {data_path}
